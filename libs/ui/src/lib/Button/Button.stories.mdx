import { Canvas, Meta, Story } from '@storybook/addon-docs';
import Button from './Button';

<Meta
  title="Button"
  component={Button}
  argTypes={{
    onClick: { action: 'clicked' },
    children: {
      control: {
        type: 'text',
      },
      defaultValue: 'Button Text',
    },
    disabled: {
      control: {
        type: 'boolean',
      },
      defaultValue: false,
    },
    variant: {
      control: {
        type: 'select',
        options: ['fill', 'outline', 'text'],
      },
      defaultValue: 'fill',
    },
    size: {
      control: {
        type: 'select',
        options: ['small', 'medium', 'large'],
      },
      defaultValue: 'medium',
    }
  }}
/>

export const Template = (args) => <Button {...args} />;

# Button

## Fill (Default)

<Canvas>
  <Story name="Fill" args={{ variant: 'fill' }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Outline

<Canvas>
  <Story name="Outline" args={{ variant: 'outline' }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Text

<Canvas>
  <Story name="Text" args={{ variant: 'text' }}>
    {Template.bind({})}
  </Story>
</Canvas>
